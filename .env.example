# SecOps Control Center - Environment Variables Template

# Database Configuration (Default: local SQLite database)
DB_TYPE=sqlite

# Optional custom database connection URL override
# DATABASE_URL=sqlite+aiosqlite:///landing.db
